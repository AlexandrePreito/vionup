'use client';

/**
 * Cópia da página NPS (interno) em /dashboard/nps_interno
 * para que o menu permaneça em "Dashboard" em vez de trocar para "NPS".
 * Mesmo conteúdo de /nps.
 */
import NPSPage from '../../nps/page';

export default function NPSInternoPage() {
  return <NPSPage />;
}
